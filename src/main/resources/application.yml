server:
  port: 8080

spring:
  profiles:
    active: dev
  application:
    name: api-music

  datasource:
    url: jdbc:postgresql://localhost:5432/musicdb
    username: music
    password: music123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
  
jwt:
  secret: super-chave-super-secreta-para-jwt-2026
  expiration: 300

minio:
  url: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket: albums



